function updateLastUserActivityTime () {
    const Promise=resolve('updateLastUserActivityTime')
    return new Promise( (resolve, reject) => 
       setTimeout(resolve,2000,'updateLastUserActivityTime')
    )
};
const Promise=fetch
('').then(res=> res.json())
function deletePost(){
    reject("delete the last post")
}

updateLastUserActivityTime()
deletePost()
